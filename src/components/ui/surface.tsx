import { Surface as HeroSurface } from 'heroui-native';

/** Elevation + background container. Base for panels and sheets. */
export type SurfaceProps = React.ComponentProps<typeof HeroSurface>;
export const Surface = HeroSurface;
